# .github/workflows/deploy-frontend.yml
name: Build and Deploy New Frontend
on:
  push:
    branches: [ main ]
  
  # Allow manual trigger
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write
  actions: read

jobs:
  build-and-deploy-frontend:
    runs-on: ubuntu-latest
        
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Regenerate package-lock.json
        run: |
          cd frontend-new
          if [ -f "package-lock.json" ]; then
            rm -f package-lock.json
          fi
          npm install --package-lock-only --no-audit
          
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          #cache: 'npm'
          #cache-dependency-path: 'frontend-new/package-lock.json'
        
      - name: Install dependencies
        run: |
          cd frontend-new
          npm install --prefer-offline
      
      - name: Run Tests
        run: |
          cd frontend-new
          npm test
      
      - name: Build
        run: |
          cd frontend-new
          npm run build

      - name: Prepare deployment
        run: |
          # Ensure bundle.css exists, create an empty one if not
          if [ ! -f "frontend-new/public/bundle.css" ]; then
            touch frontend-new/public/bundle.css
          fi

          
          # Copy papers.json to the public folder if it exists
          cp data/papers/papers.json frontend-new/public/papers.json

          ls -lha frontend-new/public/

          
      # Configure GitHub Pages
      - name: Setup Pages
        uses: actions/configure-pages@v4
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./frontend-new/public
      
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
